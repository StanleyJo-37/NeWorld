// import PropType from "prop-types";
// import "../styles/navbar.css";

// export default function ItemList({ items, numbered = false }) {

//     if (Array.isArray(items)) {
//         items = items.map(item => item);
//     } else {
//         items = Object.keys(items).map(key => items[key]);
//     }

//     if (numbered) {
//         return (
//             <ul>
//                 {
                    
//                 }
//             </ul>
//         );
//     } else {
//         return (
//             <ol>

//             </ol>
//         );
//     }
// }

// Navbar.propTypes = {
//     items: (PropType.arrayOf(PropType.any) | PropType.object).isRequired,
//     numbered: PropType.bool,
// }